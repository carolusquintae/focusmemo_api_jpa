# Informacion General del Proyecto
spring.application.name=focusmemo_api
application.version=1.0
application.descripcion=--- API REST con JPA + MySQL para la app FocusMemo ---

# Configuracion del acceso al SGBD MySQL y a la BD
spring.datasource.url=jdbc:mysql://localhost:3306/focusmemo
spring.datasource.username=admin
spring.datasource.password=admin

# Permitir que los componentes se llamen entre si de forma circular
spring.main.allow-circular-references=true

# Configuracion de Persistencia y Debug de SQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuracion de mensajes de error para la API
server.error.include-message=always
server.error.include-binding-errors=always

# Forzar driver (si es necesario)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Permitir conexiones a la API desde cualquier dispositivo
server.address=0.0.0.0